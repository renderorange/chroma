name: CI Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Run lint tests
      run: |
        chmod +x test/run_lint_tests.sh
        ./test/run_lint_tests.sh
